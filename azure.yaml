# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: aspire-demo
services:  
  app:
    language: dotnet
    project: ./aspire-demo.AppHost/aspire-demo.AppHost.csproj
    host: containerapp
